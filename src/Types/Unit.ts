export type Unit = {
    id: string
    name: string
    isActive: boolean
    lastUpdated: Date
    type: string
}

export type RequestUnit = {
    name: string
    isActive: boolean
    type: string
}
